{
  "name": "task-manager-monorepo",
  "version": "1.0.0",
  "scripts": {
    "start": "npm --prefix ./server start",
    "build": "npm install && vite build && npm --prefix ./server install"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.2.1",
    "vite": "^5.0.8"
  }
}
