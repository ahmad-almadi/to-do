[phases.setup]
nixPkgs = ["nodejs_18"]

[phases.install]
cmds = [
  "npm install",
  "cd server && npm install && cd .."
]

[phases.build]
cmds = [
  "npm run build",
  "mkdir -p server/dist",
  "cp -r dist/* server/dist/",
  "ls -la server/dist"
]

[start]
cmd = "npm start"
