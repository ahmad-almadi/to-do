[phases.setup]
nixPkgs = ["nodejs_18"]

[phases.install]
cmds = [
  "cd .. && npm install && cd server",
  "npm install"
]

[phases.build]
cmds = [
  "cd .. && npm run build && cd server",
  "ls -la ../dist || echo 'Dist not found'"
]

[start]
cmd = "node src/db/migrate.js && node src/index.js"
