{
  "name": "task-manager-monorepo",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "install:all": "cd frontend && npm install && cd ../backend && npm install",
    "build:frontend": "cd frontend && npm install && npm run build",
    "start": "cd backend && node src/db/migrate.js && node src/index.js"
  }
}
