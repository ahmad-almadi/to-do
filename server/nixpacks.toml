[phases.setup]
nixPkgs = ["nodejs_18"]

[phases.install]
cmds = [
  "cd .. && npm install",
  "npm install"
]

[phases.build]
cmds = [
  "cd .. && npm run build",
  "ls -la ../dist"
]

[start]
cmd = "node src/db/migrate.js && node src/index.js"
